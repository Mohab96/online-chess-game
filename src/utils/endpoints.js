const VERSION = "/api";
const AUTH = `${VERSION}/auth`;
const CHATS = `${VERSION}/chats`;
const FRIENDS = `${VERSION}/friends`;
const GAMES = `${VERSION}/games`;
const PLAYERS = `${VERSION}/players`;
const SETTINGS = `${VERSION}/settings`;

module.exports = { AUTH, CHATS, FRIENDS, GAMES, PLAYERS, SETTINGS };
